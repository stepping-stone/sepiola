
#cmakedefine PORTABLE_INSTALLATION

#define APPLICATION_SHARE_DIR "${APPLICATION_SHARE_DIR}"

#define SSBACKUP_EXECUTABLE_NAME "${SSBACKUP_EXECUTABLE_NAME}"

#define SSBACKUP_VERSION "${CPACK_PACKAGE_VERSION}"

#cmakedefine IS_RESELLER
